---
description: |-
  Please see `../config/subsetsExamples.yaml` for details.
  Additionally this preference file has a `skipDefaults` option which can be set
  to `true` to skip the default examples in `../config/subsetsExamples.yaml`.
skipDefaults: false
examples:
  - label: "Sarcoma Cancer Cell Lines"
    note: "Sarcoma cell lines"
    description: |
      Some sarcoma cell lines.
    parameters:
      allTermsFilters:
        defaultValue:
          - cellosaurus:CVCL_0312
          - cellosaurus:CVCL_0317
          - cellosaurus:CVCL_0548
          - cellosaurus:CVCL_B450
